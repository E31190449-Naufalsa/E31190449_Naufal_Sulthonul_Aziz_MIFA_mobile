<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apilikasi">
    // digunakan untuk memberikan icon pada aplikasi saat di instal kedalam smartphone
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SQLite"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Apilikasi">
        //berguna untuk  memeberikan nama bar pada tampilan aplikasi
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        //berguna untuk  memeberikan label pada tampilan aplikasi
        <activity android:name=".UpdateBiodata"
            android:label="UPDATE BARU">
        </activity>
        //berguna untuk  memeberikan label pada tampilan aplikasi
        <activity android:name=".ViewBiodata"
            android:label="LIHAT DATA">
        </activity>>
        //berguna untuk  memeberikan label pada tampilan aplikasi
        <activity android:name=".Biodata"
            android:label="FORM BIODATA BARU">
        </activity>>
    </application>
</manifest>